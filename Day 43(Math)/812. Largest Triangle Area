//812. Largest Triangle Area
//https://leetcode.com/problems/largest-triangle-area/description/?envType=daily-question&envId=2025-09-26

/*We know the normal formula:

  ------------Area=1/2​×base×height

But when you only have coordinates of points, finding height is messy. So we use determinants (shoelace formula), which secretly does the same thing.

Step 1: Take three points
Let’s say the triangle has points:

  A(x1​,y1​),B(x2​,y2​),C(x3​,y3​)

Step 2: Formula for area of polygon from coordinates
The shoelace formula for a triangle is:

 ---------- Area=1/2|​x1​(y2​−y3​)+x2​(y3​−y1​)+x3​(y1​−y2​)|

*/
​


class Solution{
    public double largestTriangleArea(int[][] points) {
        double ans=-1;
        for(int i=0;i<points.length;i++){
            for(int j=i+1;j<points.length;j++){
                for(int k=j+1;k<points.length;k++){
                    int x1=points[i][0];
                    int y1=points[i][1];
                    
                    int x2=points[j][0];
                    int y2=points[j][1];
                
                    int x3=points[k][0];
                    int y3=points[k][1];
                    ans=Math.max(ans,0.5*Math.abs(
                        x1*(y2-y3)+
                        x2*(y3-y1)+
                        x3*(y1-y2)
                        ));
                    // double temp=0.5*Math.abs((points[i][0]*(points[j][1]-points[k][1]))+(points[j][0]*(points[k][1]-points[i][1]))+(points[k][0]*(points[i][1]-points[j][1])));
                    // if(ans<temp){
                    //     ans=temp;
                    // }
                }
            }
        }
        return ans;

    }
}
